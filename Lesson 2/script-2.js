'use strict'

//Задание 2

let a = 2;
let x = 1 + (a *= 2);
//сначала выполнится код в (), т.е. переменная а умножится на 2, 
//затем это значение присвоится в переменную а, 
//затем выполнится сложение, т.е. к 1 прибавится значение переменной а = 4;